# Default values for harvester-helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
#DNSserver: # optional if you need it.

uname: harvester
gname: harvester
harvester_id: boxed_test_02
ProxyPath: /usr/etc/panda/proxy

mariadb:
  clusterIP: 10.96.0.101 # must be in subnet of K8s cluster.
  RootPassword: FIXME
  Password: FIXME
