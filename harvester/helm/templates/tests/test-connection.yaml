apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "harvester-helm.fullname" . }}-test-connection"
  labels:
    {{- include "harvester-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "harvester-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
