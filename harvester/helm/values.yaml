# Default values for harvester-helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
proxy: aa
uname: harvester
gname: harvester
harvester_id: boxed_test_02
ProxyPath: /usr/etc/panda/proxy
database:
  host: 
  user:
  paswword:
  schema:

mariadb:
  deploy: false
  RootPassword: FIXME
DNSserver: # optional if you need it.
